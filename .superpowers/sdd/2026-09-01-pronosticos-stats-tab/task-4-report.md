# Task 4 report — Cálculos puros — Eliminatorias y Plantilla extras

- Test: `tests/pronosticosElimsPlantilla.test.js` created per brief, FAIL verified (`calcEliminatoriasPronosticosStats is not a function`), then PASS after implementation (`OK elimsPlantilla`).
- Impl: `js/stats.js` added `calcEliminatoriasPronosticosStats(finalPredictionsCache)` reusing `tallyChampions` (championTally), Map tally for final pair `champion-runnerUp` (finalTally sorted by votes), Map tally for `semiFinalists` (semisFreq), and `compareQuadroWithCommunity` for `quadro` (fallback to first user when no currentUser). Added `calcPlantillaPronosticosExtras(aggOrPts, squadsCache)` accepting both `aggregateSquads` result or raw points map: groups `agg.rows` by `posicion` into `top3ByPos` sorted by `possessionPct`, counts picks per `equipo` for `equipoMas`/`equipoMenos`, filters `possessionPct >=70`/`<=30` for `imprescindible`/`diferencial`, and computes per-user avgCommon intersection to derive `hipster` (min) / `mainstream` (max). Exported both via `global` and `module.exports` (vanilla, no matchStats needed, pure).
- Verify: `node tests/pronosticosElimsPlantilla.test.js` → PASS; `for f in tests/pronosticos*.test.js tests/estadisticas*.test.js; do node $f; done` → all PASS (pronosticosPartidos, pronosticosClasificacion, estadisticas-subtabs, etc.) no regressions.
- Commit: `3d61744 feat(stats): add eliminatorias+plantilla pronosticos extras pure` (includes `js/stats.js` + `tests/pronosticosElimsPlantilla.test.js`).
- Fix: review finding — `calcEliminatoriasPronosticosStats` removed `quadro._note` mutation (line 580). Now returns pure `{users,avgCommon,avgPct,veryDiffCount}` via shallow copy of `compareQuadroWithCommunity` result, keeping fallback to first user but no extra property and no global mutation. Verified `Object.keys(quadro).length===4`, `_note` absent, `compareQuadroWithCommunity` return unmutated, cache not mutated. Re-ran `node tests/pronosticosElimsPlantilla.test.js` → PASS; `tests/pronosticos*.test.js` + `tests/estadisticas*.test.js` all PASS.
